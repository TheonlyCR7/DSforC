// 链表的实现
#include <stdio.h>
#include <stdlib.h>

// 定义单个节点
struct LNode{
    int data; 
    struct LNode *next;
}LNode, *LinkList;  // 同时定义结构体LNode, 结构体指针LinkList

// 输入LNode结构体指针  返回LNode结构体
struct LNode List_HeadInsert(struct LNode *L){
    struct LNode *s;
    int x;
    // l = (LinkList)malloc(sizeof(LNode));
}





